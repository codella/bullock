require 'bullock/parse/item_sets_dfa'

describe Bullock::Parse::ItemSetsDfa do
  let(:item_sets_dfa) { Bullock::Parse::ItemSetsDfa.new }
  let(:grammar) { double(:grammar) }

  describe "#process" do
    it "does" do

    end
  end
end
